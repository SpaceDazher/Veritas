# Set a provisioned PostgreSQL connection locally. Never commit real values.
DATABASE_URL=
# Optional CLI endpoint; localhost is a local demo, not an external experiment.
VERITAS_URL=http://localhost:3000
